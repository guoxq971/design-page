/**
 * three的meshList的item
 * @typedef {Object} meshItem
 * @property {three.mesh} mesh mesh
 * @property {HTMLCanvasElement} canvas canvas
 * @property {ParseViewItem|null} view view
 * */
export class MeshItem {
  mesh;
  canvas;
  view;
}
