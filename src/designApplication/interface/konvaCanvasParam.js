import { ParseViewItem } from '@/designApplication/interface/parseViewItem';

/**
 * konva canvas 参数
 * @class KonvaCanvasParam
 * @property {string} id 容器id
 * @property {ParseViewItem} view 视图数据
 * @property {StaticViewItem} staticView 静态视图数据
 * */
export class KonvaCanvasParam {
  id;
  view;
  staticView;
}
